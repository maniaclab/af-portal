Flask               # Flask is our web framework.                    
flask_wtf           # We use flask_wtf for its CSRF protection feature
jinja2_markdown     # This Jinja extension lets us write Markdown in our templates.              
globus-sdk==1.7.1   # Lets us authenticate users based on their institutional login or their Globus ID.
kubernetes          # A Python binding for the Kubernetes client. It is a lot like kubectl.   
pyyaml              # Lets us parse YAML files into Python dictionaries.                             
matplotlib          # We use Matplotlib for data visualization.                  
pandas              # We use pandas because it has some useful functions for working with data.                  